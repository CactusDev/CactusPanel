
import * as React from "react";

import { BrowserRouter as Router, Route, Link } from "react-router-dom";

export class Commands extends React.Component {

    public render() {
        return (
            <div>
                I have so many commands, that you wouldn't believe it
            </div>
        );
    }
}
